<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required to call Google's OAuth servers -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required to list Google accounts on the device -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <application
            android:allowBackup="true"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/Theme.SpotMyDime">

        <!-- Screen 1: First thing user sees when app opens -->
        <activity
                android:name=".ui.OnboardingActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Screen 2: Gmail permission explainer + OAuth trigger -->
        <activity
                android:name=".ui.ConnectGmailActivity"
                android:exported="false" />

    </application>

</manifest>